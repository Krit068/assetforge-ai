# 第二阶段前端开发文档｜真实任务闭环

> 文档版本：v1.2  
> 创建日期：2026-09-01  
> 配套文档：《前端技术适配声明》《第一阶段技术开发文档｜Alpha 核心纵向切片》  
> 阶段定位：在现有 Alpha 工作台骨架上完成第一条可恢复、可验证的任务闭环，不扩大到商业化、社区或团队协作。

## 1. 本阶段目标

用户提交一次 3D 生成后，可以看到后端真实任务阶段和渐进候选；网络断开或页面刷新后能按任务 ID 恢复；用户取消时调用后端真实取消接口；Mock 自动验收不触发任何付费模型。

## 2. 范围

### 本阶段包含

- 生成工作台现有页面的任务状态升级。
- 后端状态到统一前端状态的显式映射。
- 任务 ID 写入 URL，刷新和返回时按 ID 恢复。
- SSE 实时事件、断线回查和轮询降级。
- 后端真实任务取消。
- 一次用户意图一个稳定幂等键。
- 中英文偏好持久化，切换语言不丢失任务。
- API 错误码、诊断 ID 和未知状态安全兜底。
- Vitest/Testing Library 和 Playwright 最低自动化检查。

### 本阶段不包含

- 新增支付、额度、团队、社区或公开分享。
- 完整任务中心、资产库和多项目管理页面。
- 新增真实模型调用预算或批量效果测试。
- 资产版本、结构化 QA 和导出 API 的后端实现。
- 外部用户认证、租户隔离和对象存储。

## 3. 用户流程

1. 用户输入描述或上传参考图，完成必要追问和生成前确认。
2. 前端复用或创建 Alpha 项目，使用稳定幂等键提交一次生成任务。
3. 后端返回任务后，前端立即将任务 ID 写入 `?task={taskId}`。
4. 前端优先订阅 SSE，展示队列、几何、材质、后处理、QA 和候选增量。
5. SSE 断开时，前端说明任务未必停止，并按任务 ID 查询真实状态；必要时使用有限轮询。
6. 用户可调用后端取消接口；取消成功后保留已有信息。
7. 任务进入 Ready、Needs Fix、Failed 或 Cancelled 后停止订阅和轮询。
8. 用户刷新页面，前端从 URL 恢复同一任务、候选、参考图和诊断信息。

## 4. 页面状态矩阵

| 模块 | 初始 | 提交/队列 | 运行/流式 | 等待确认 | 成功 | 部分成功 | 失败 | 断线/恢复 | 取消 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 生成表单 | 可编辑 | 禁止重复提交 | 禁止重复提交，可取消 | 显示明确确认卡 | 保留输入 | 保留输入 | 保留输入 | 保留输入 | 可重新开始 |
| 任务状态 | 无任务 | DRAFT / VALIDATING / QUEUED | PREPROCESSING / GEOMETRY / TEXTURING / POST_PROCESSING / QA | Prompt/概念图/3D 调用确认 | READY | NEEDS_FIX | FAILED + 诊断 ID | disconnected → 查询 → SSE/轮询 | CANCELLED |
| 候选区 | 空提示 | 等待候选 | 增量显示、去重 | 不创建候选 | 展示成功候选 | 成功和失败候选并存 | 不伪造产物 | 保留已持久化候选 | 保留已有候选 |

## 5. 接口契约

- `GET /api/v1/capabilities`：读取供应商和候选能力。
- `GET /api/v1/projects`、`POST /api/v1/projects`：复用或创建 Alpha 项目。
- `POST /api/v1/prompts/analyze`：生成前清晰度检查。
- `POST /api/v1/generation-tasks`：携带 `Idempotency-Key` 创建任务。
- `GET /api/v1/generation-tasks/{taskId}`：查询并恢复真实状态。
- `GET /api/v1/generation-tasks?limit=20`：任务中心读取最近任务及其候选资产。
- `GET /api/v1/generation-tasks/{taskId}/events`：消费 `chunk* → done | error`。
- `POST /api/v1/generation-tasks/{taskId}/cancel`：真实取消任务。

前端错误对象归一化为 `code + message + diagnosticId + details`。未知状态映射为 `stale`，不得静默视为成功或失败。

## 6. 实现方案

- `lib/api.ts` 负责集中 HTTP、错误归一化、项目复用、任务创建/查询/取消和 SSE。
- `lib/task-state.ts` 负责纯状态映射、终态判断和中英文状态文案。
- `TaskStatusCard` 展示真实阶段、连接恢复说明、任务 ID 和取消入口。
- 页面本地状态只保存表单、当前候选、语言和连接展示状态；任务事实以后端为准。
- 语言偏好存入 `localStorage`，任务 ID 存入 URL；二者不保存或冒充任务结果。
- SSE 失败只触发状态恢复，不自动重新创建付费任务。

## 7. 验收标准

- 同一次用户确认只创建一个任务；不确定是否创建成功时复用原幂等键。
- URL 出现任务 ID；复制或刷新后恢复同一任务。
- Queued 与 Running 文案不同；运行中展示后端实际阶段。
- SSE 断开后显示恢复提示并查询后端，不能直接显示失败。
- 取消按钮调用后端取消接口；不能用关闭浏览器请求冒充取消任务。
- Ready、Needs Fix、Failed、Cancelled 均停止实时连接和轮询。
- 切换语言后当前任务、候选和输入不被重置，刷新后语言保持。
- 自动检查通过：Lint、TypeScript、Vitest、Playwright、生产构建。
- Playwright 测试不得调用真实付费模型。

## 8. 风险与回退

- **SSE 受代理缓存或跨域限制**：先回查任务，再退化为 3 秒有限轮询；终态立即停止。
- **Vinext beta 兼容风险**：以生产构建和 Sites/Cloudflare Worker 实际部署验证为准。
- **旧任务没有新版概念图质量记录**：只读展示并明确标记，禁止直接当作已通过质量门。
- **后端未实现导出契约**：继续保持导出按钮禁用，不用候选文件 URL 冒充正式导出。
- **真实供应商可能产生费用**：自动化浏览器测试拦截 API 并使用固定 Mock；真实冒烟必须单独授权。

## 9. 本轮交付结果

- 已实现统一任务状态、任务 URL、SSE、轮询恢复、取消和稳定幂等键。
- 已实现语言偏好持久化和 API 错误归一化。
- 已新增任务状态组件及其自动化测试。
- 已新增 15 个前端单元/组件断言和 2 条浏览器流程。
- 后端现有 44 个测试通过；前端 Lint、类型检查、单元/组件测试、浏览器测试和生产构建通过。
- 当前仍未完成正式资产/版本、结构化 QA 和导出 API，因此第二阶段完成的是“真实任务与候选闭环”，不是“完整资产交付闭环”。

## 10. 科技视觉与动效升级（v1.1）

### 10.1 参考提炼

- React Bits Cubes：采用深色空间、规则立方体阵列和局部波峰响应，不复制其页面结构或代码。
- Saaspo 产品页集合：采用高密度工作台、窄边框、克制圆角和清晰左右信息分区。
- MotionSites Agent Wave：采用慢速环境呼吸、扫光和青紫渐变，快速反馈只在悬停、点击和任务状态变化时出现。

### 10.2 本项目视觉规则

- 主背景为近黑蓝，卡片采用低透明深蓝；主强调色为电光青，紫色只作为空间辅助光。
- 中央 3D 画布是主视觉，不在工作台前增加营销 Hero 或与生成任务无关的装饰内容。
- 3D 场景增加 81 个 InstancedMesh 立方体节点；波峰同时受时间和画布指针影响，保持单次绘制调用，避免 81 个独立 React Mesh。
- 预览区增加慢速环境光、周期扫光、实时节点读数和候选卡悬停反馈；任务卡增加脉冲状态点和四阶段进度轨道。
- 动效只表达“可交互空间、实时连接、任务推进”三种含义，不用持续抖动、弹跳或高频闪烁吸引注意。

### 10.3 可访问性与性能边界

- 系统开启 `prefers-reduced-motion: reduce` 时，CSS 动效近似静止，3D 波场固定在稳定帧。
- 任务阶段轨道使用 `role="progressbar"` 和可读的阶段数值；纯装饰元素均从可访问性树隐藏。
- 指针光场直接更新 CSS 自定义属性，不触发 React 状态重渲染；3D 节点使用实例化网格。
- 交互效果不改变生成、取消、刷新恢复、SSE/轮询或 QA 的数据契约。

## 11. 工作台交互补全（v1.2）

- 左上角菜单接入生成器、任务中心、资产库、项目规格和帮助中心导航。
- 任务中心读取最近 20 个持久化任务，支持刷新、打开恢复和取消运行中任务。
- 资产库聚合任务候选，支持打开候选并同步中央 3D 预览和右侧 QA 指标。
- 帮助中心支持工作流、断线恢复、模型检查和快捷键内容搜索。
- 项目规格弹窗支持质量档位调整与保存反馈；项目展示名统一为 `Unity URP Mobile`，不再使用示例项目名“青铜遗迹”。
- 预览工具补齐 PBR/黏土材质切换、线框快捷键和相机复位。
- 导出 GLB 与 Unity 接入继续保持禁用，并明确说明需等待资产版本接口，避免伪交互。

## 12. Agent 生成器双阶段改版（v1.3）

### 12.1 参考证据与设计判断

- `证据-001`【已确认】：用户提供的 Meshy 截图以单一大输入框作为首要任务入口，输入区同时容纳图片上传、模式选择、语音和发送动作。
- `证据-002`【已确认】：快捷意图位于主输入区下方，用于帮助用户快速建立输入方向。
- `证据-003`【已确认】：用户要求提交后进入左右分栏，左侧继续交流，右侧承载图片生成。
- `证据-004`【已确认】：参数截图显示面数 `1,993,492`、顶点数 `1,060,326`。
- 【合理推断】：参考产品将“表达意图”和“执行/迭代”拆成两个界面状态，可降低首屏认知负担。
- 【建议设计】：AssetForge 采用同类任务结构，但保留自己的科技视觉、生成确认和 Unity Mobile 交付约束，不复制参考产品品牌与页面文案。

### 12.2 实现结果

- 生成器首屏改为居中 Agent Composer，支持文字、参考图、快捷意图、回车提交和显式发送按钮。
- 提交后切换为两栏生成工作区：左栏展示用户需求、Agent 状态、澄清问题、费用/路线确认和继续输入；右栏展示用户参考图、概念图、多视图和后续 3D 候选。
- 保留任务中心、资产库、帮助、设置、任务恢复、取消、SSE/轮询和幂等提交，不因界面重排删除真实任务能力。
- 语音按钮当前只提供明确的“尚未开放”反馈，不伪造语音识别结果。

### 12.3 高模能力边界

- 【已确认】：Tripo v3.1 Standard 的 `face_limit` 最高为 1,500,000，Ultra（`geometry_quality=detailed`）最高为 2,000,000；因此 `1,993,492` 面位于官方上限内。
- 【已确认】：高模档位现已改为直接请求并保留 Tripo v3.1 Ultra 的最高 2,000,000 面结果，不再自动调用 Smart LowPoly。
- 【未知】：API 不提供“精确顶点数”目标控制，`1,060,326` 顶点不能在提交前承诺，必须下载实际 GLB 后由检查器实测。
- 【已确认】：生成器默认选择“高模源文件（最高 2,000,000 面）”；用户可在首屏、生成工作区或项目规格中切换为“游戏就绪（最高 20,000 面）”。
- 【建议设计】：高模用于烘焙、归档和二次拓扑，游戏就绪版本用于 Unity Mobile 运行时；两者不再在同一任务中强制串行生成。
