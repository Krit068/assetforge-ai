# 前端技术适配声明

> 文档版本：v1.0  
> 适配日期：2026-09-01  
> 适配范围：AssetForge AI 第一阶段 Alpha 核心纵向切片  
> 声明性质：基于现有 PRD、阶段开发文档、前端技术栈手册、后端接口实现与当前前端代码的适配审计；作为后续前端开发与验收基线。

## 1. 项目判断

- **产品类型**：面向游戏内容生产的 AI Agent 3D 素材生成工作台。产品通过异步任务完成提示词澄清、概念图生成、3D 候选生成、技术 QA 与资产导出。
- **主要用户**：首阶段优先服务独立游戏开发者、小型游戏团队主创和技术美术；制作人、美术负责人及 API 开发者属于后续扩展用户。
- **主要终端**：桌面 Web 为完整创作与验收终端；移动 Web 仅保证打开、查看状态与基础审批，不承诺完整 3D 编辑。
- **项目性质**：已有项目迭代，不重新搭建或整体迁移前端工程。
- **本阶段业务目标**：跑通“创建项目 → 输入中文/英文描述或上传参考图 → 清晰度检查与选项式追问 → 确认概念图 → 创建 3D 任务 → 查看真实阶段与候选 → 3D 预览与技术 QA → 导出 GLB → Unity URP Mobile 验证”的首条可恢复纵向闭环。
- **首条验收样例**：静态道具、Unity URP Mobile、简体中文默认、GLB 默认输出；默认 4 个候选，真实供应商受能力和成本约束时可降为 1 个。
- **阶段边界**：本阶段不实现支付、套餐、额度钱包、公开社区、完整团队协作、批量生产、引擎插件、复杂角色动画或其他非 3D 素材能力。

## 2. 输入资料

- **PRD**：已读取《AI游戏素材生成产品_PRD_v0.9.md》。
- **前端手册**：已完整读取《AI产品Vibe Coding通用前端技术栈手册.md》。
- **分阶段开发文档**：已读取《第一阶段技术开发文档_Alpha核心纵向切片.md》；本文不另写一套阶段范围，以该文档为主并补充前端适配结论。
- **后端与接口资料**：已有 FastAPI 路由、Pydantic Schema、任务状态机和阶段接口约定；当前实现可作为 Alpha 联调契约，但尚未形成独立、稳定、可生成客户端的 OpenAPI 版本基线。
- **设计稿**：未发现独立设计稿或视觉参考；当前代码已经形成暖色工业/游戏资产工作台视觉方向，后续优先延续现有设计变量，不另起视觉体系。
- **现有代码**：已有单页生成工作台、提示词追问、参考图上传、概念图确认、3D 任务轮询、候选展示、React Three Fiber 预览、基础 QA 展示和中英文文案。
- **当前事实边界**：导出接口、前端取消入口、SSE 客户端、任务 URL 恢复、完整国际化持久化及前端自动化测试尚未完成；不得把禁用按钮、Mock 候选或后端已有接口视为前端闭环已完成。

## 3. 技术选择

### 3.1 框架与版本

- 继续沿用现有 **Node.js 22 LTS** 工程，`package.json` 最低版本为 Node.js 22.13.0。
- 继续采用 **React 19.2.8、TypeScript 5.9.3 strict、App Router 目录约定**。
- 当前构建运行层为 **Vinext 1.0.0-beta.8 + Vite 8.2.2**，并使用 `@openai/sites-vite-plugin` 与 Cloudflare Workers 兼容构建；本阶段不擅自迁移为另一套脚手架。
- 继续采用 **Tailwind CSS 4.2.1**、CSS 语义变量、shadcn/Base UI 基础组件和 Lucide 图标。
- 3D 预览继续采用 **Three.js 0.185.x + React Three Fiber 9.7.x + Drei 10.7.x**，按需客户端加载，不参与服务端首屏渲染。
- 保留 `package-lock.json` 作为可重复安装依据；同一 Alpha 阶段不主动跨大版本升级。

### 3.2 样式与组件策略

- 复用现有 `components/ui` 基础组件和 `globals.css` 设计变量，业务组件围绕上传、追问、确认、任务状态、候选比较、3D 预览、QA 和导出拆分。
- 当前 `app/page.tsx` 已承担过多业务和状态职责；后续按业务功能逐步拆分，但不为满足目录模板进行无业务收益的大重构。
- 延续暖色金属、深色 3D 画布和高密度工作台风格；先在现有代表页上收敛颜色、圆角、密度和字体，再扩展多路由页面。
- 中文字体使用 Geist 拉丁字形加 `PingFang SC / Microsoft YaHei` 系统回退，不打包无中文字形或授权不明的商业字体。

### 3.3 数据请求与状态策略

- 所有 HTTP 请求继续集中在 `frontend/lib/api.ts` 或后续拆分出的 Feature API 层，页面组件不得直接散落请求地址和错误解析。
- 后端数据库中的项目、任务、候选、QA 和导出记录是唯一业务事实来源；浏览器状态只保存表单草稿、弹窗、当前选中候选和语言偏好。
- 当前复杂度下不引入 TanStack Query、Zustand 或 Redux。出现多路由共享缓存、重复请求或复杂失效逻辑后，再评估 TanStack Query；只有跨远距离组件共享大量纯客户端状态时才评估 Zustand。
- 外部响应进入业务层前应补运行时校验和统一错误结构；当前仅依赖 TypeScript 类型断言与 `Error(message)`，不足以覆盖后端字段漂移、未知枚举、诊断 ID 和字段级错误。
- 可分享、可恢复状态应进入 URL。任务详情目标入口采用 `/tasks/{taskId}`，或在本阶段最小化为 `/?task={taskId}`；不得继续只依赖 `/latest` 恢复最近一次全局任务。

### 3.4 流式通信策略

- 任务创建使用普通 HTTP，必须携带幂等键；任务渐进进度和候选返回优先使用后端现有 SSE `GET /api/v1/generation-tasks/{taskId}/events`。
- SSE 事件遵循 `chunk* → done | error`；断流不等于任务失败。连接中断后先按任务 ID 查询后端状态，再决定重连或进入有限轮询。
- 保留轮询作为恢复和兼容降级方案：页面不可见时降低频率，终态立即停止，失败采用退避，不允许多个组件轮询同一任务。
- 当前页面实际采用 150ms（Mock）或 3s（真实供应商）轮询，尚未消费 SSE；这是待补齐项，不是最终通信方案。

### 3.5 表单与国际化

- 当前表单规模可继续使用受控 React State 和后端校验，不立即引入 React Hook Form；当项目规格成为多步骤动态表单后，再采用 React Hook Form + Zod。
- 简体中文为默认语言，英文为必备切换语言。Alpha 可继续使用轻量文案字典，但需拆出页面组件、错误、状态和 QA 共用的语言资源，并把语言偏好持久化。
- 用户 Prompt、资产名和文件名不自动翻译；缺失翻译键、界面中英混排均视为测试失败。

### 3.6 测试与交付门槛

- 保留 **Oxlint** 作为当前静态检查工具，并新增明确的 `typecheck` 脚本（`tsc --noEmit`）。
- 按阶段文档补齐 **Vitest + React Testing Library**，覆盖状态映射、表单、追问、确认、失败、恢复、QA 阻断和 3D 降级状态。
- 补齐 **Playwright** 核心 E2E，至少覆盖一条 Mock 全闭环和一条真实后端冒烟路径。
- 每个前端交付阶段至少执行 `npm run lint`、`npm run typecheck`、`npm run test`、`npm run build`，并真实检查 390、768、1280、1440px 布局以及 Console、Network、断线与刷新恢复。
- 当前工程只有 `lint` 和 `build` 脚本，尚未配置 `typecheck`、`test` 与 E2E；在补齐并通过前不得声称达到前端手册完整交付门。

### 3.7 部署环境

- 前端按现有 **OpenAI Sites / Cloudflare Workers** 兼容工程部署，`frontend/.openai/hosting.json` 当前未声明 D1 或 R2；业务数据库与资产事实不迁入前端托管层。
- 后端 FastAPI、PostgreSQL、Worker 和受控资产存储保持独立部署，前端通过 HTTPS API 访问。
- 浏览器仅允许读取公开 API Base URL 等公开配置；模型密钥、数据库凭证、供应商凭证和私密业务规则只存在于后端环境。
- 上线前必须确定同源代理或明确的 CORS、Cookie、CSRF 方案。当前 `NEXT_PUBLIC_API_BASE_URL` 直连后端只适合本地或受控 Alpha，不等于生产鉴权方案。
- 本项目不使用手册中针对官方 Next.js Node standalone 的打包方式；最终交付以 Vinext/Sites 的 Cloudflare Worker 兼容构建与真实线上请求验证为准。

## 4. Agent 状态

### 4.1 后端状态到前端统一状态的映射

| 后端/流程状态 | 前端统一状态 | 用户界面含义 |
| --- | --- | --- |
| 尚未提交 | `idle` | 可编辑输入并检查提交条件 |
| Prompt 信息不足、概念图待确认、3D 调用待确认 | `waiting_user` | 明确展示需要决定的内容、推荐项、影响与返回方式 |
| `DRAFT`、`VALIDATING` | `submitting` | 正在校验和受理；禁止重复提交 |
| `QUEUED` | `queued` | 已进入队列，不能伪装为正在生成 |
| `PREPROCESSING`、`GEOMETRY`、`TEXTURING`、`POST_PROCESSING`、`QA` | `running` | 展示真实阶段、已持久化候选和取消边界，不伪造精确倒计时 |
| SSE 候选或进度增量到达 | `streaming` | 增量展示并去重，不覆盖已成功候选 |
| `READY` | `succeeded` | 展示真实产物、QA 结果和下一步操作 |
| `NEEDS_FIX`，或候选成功与失败并存 | `partially_succeeded` | 保留成功产物，说明阻断项、失败项和修复入口 |
| `FAILED` | `failed` | 展示用户可理解原因、错误码/诊断 ID 和是否可重试 |
| `CANCELLED` | `cancelled` | 保留已持久化信息，允许复制任务或重新开始 |
| SSE/网络中断 | `disconnected` | 明确说明任务未必停止，并查询后端恢复 |
| 本地状态可能过期或出现未知后端枚举 | `stale` | 显示安全兜底并主动刷新，不静默映射为成功或失败 |

候选自身的 `pending / running / ready / failed / cancelled` 必须独立展示；单个候选失败不得隐藏其他成功候选。

### 4.2 用户确认方式

- Prompt 不清晰时最多提出 3 个有针对性的问题，每题提供方向选项、说明和“其他/自定义”；回答完成前不创建付费 3D 任务。
- 文字输入先进入 Seedream 概念图确认；人物模式展示正、左、后、右四视图及独立配件，道具模式展示单图。概念图未通过质量门或未由用户确认时不调用 3D 模型。
- 提交 Tripo/HY-3D 前显示输入摘要、资产类型、质量档、候选数、供应商链路、预计图片数量和费用影响；按钮使用“确认并生成 3D”等明确业务文案。
- 涉及真实模型费用、导出、覆盖或删除时必须再次说明影响；前端确认不能替代后端权限、幂等和状态校验。

### 4.3 取消、重试与断线恢复

- “取消任务”只在后端真实取消接口成功后显示为已取消；若只是关闭浏览器请求，文案应为“停止等待”或“离开页面”。当前后端已有取消接口，前端尚未接入。
- 创建付费任务不自动重试。网络超时后先用原幂等键或已获得的任务 ID 查询是否已创建，再决定是否重试；当前每次调用即时生成 UUID 的实现需要调整为一次用户意图一个稳定幂等键。
- 状态查询可进行有限、退避式重试；SSE 断线后先查询任务，再恢复订阅或轮询。
- 刷新、返回或重新打开页面时，从 URL 读取任务 ID 并向后端恢复真实状态、候选和产物，不重复建项或创建任务。

## 5. 页面、接口与产物

### 5.1 页面范围

- **当前代表页**：单页生成工作台，包含输入、上传、提示词追问、概念图确认、候选区、3D 预览与 QA 摘要。
- **本阶段核心页面/视图**：项目列表与建项、生成工作台、任务详情与候选、候选比较、资产详情、导出面板。
- 可以在 Alpha 内合并页面外壳，但任务和资产必须拥有稳定 URL；不能以一个不断增长的客户端组件替代清晰的信息架构。
- 桌面端完成全流程；移动端重排为可查看、可审批的纵向布局，避免固定三栏和 3D 画布撑破屏幕。

### 5.2 核心接口

- 当前已实现：能力查询、项目创建/查询、Prompt 分析、参考图上传/删除、概念图创建/恢复、任务创建/查询/取消、任务 SSE。
- 本阶段仍需补齐或冻结：资产/版本、结构化 QA、导出创建与下载、短时授权下载，以及与任务/候选关联的稳定响应契约。
- 前端内部统一错误对象至少包含 `code`、`userMessage`、`retryable`、`diagnosticId/requestId` 和可选字段错误；未知错误与未知状态必须有安全兜底。
- 后端契约稳定后再评估 OpenAPI 类型生成；在此之前继续保留手写类型和业务适配层。

### 5.3 产物与预览

- **图片产物**：用户参考图、Seedream 概念图、人物四视图与独立配件图；提供占位、失败、放大、原图查看和移除/替换。
- **3D 产物**：候选 GLB；使用 React Three Fiber 实现旋转、缩放、线框和自适应取景，加载失败时退化为缩略图或明确错误，不用示意模型冒充真实候选。
- **QA 产物**：展示检测值、目标值、Pass/Warning/Block 和修复建议；缺少检测值必须显示 Pending/未检测，不能显示 Pass。
- **导出产物**：Alpha 以 GLB 和包含 manifest、QA、license 的导出包为主；只有真实下载接口成功并记录导出结果后才显示完成。

### 5.4 核心验收闭环

1. 创建 Unity URP Mobile 静态道具项目。
2. 输入中文描述或上传合法参考图；描述不足时完成追问。
3. 审核概念图和实际费用信息，确认后创建一次 3D 任务。
4. 看到 `queued`、真实运行阶段、渐进候选、失败或部分成功状态。
5. 选择候选并在浏览器检查 GLB、实测指标和结构化 QA。
6. QA Block 时禁止导出并解释原因；满足完成门后下载真实 GLB/导出包。
7. 刷新任务 URL 后恢复同一任务和产物，不发生重复创建或重复付费。
8. 在 Unity URP Mobile 样例工程完成人工导入验证。

### 5.5 响应式与无障碍目标

- 验收宽度：390px、768px、1280px、1440px。
- 目标浏览器：桌面 Chrome、Edge、Safari 最近两个主要版本；Firefox 尽力支持；移动端检查常见 iPhone 与 Android 浏览器的查看路径。
- 核心表单有 Label，按钮有可访问名称，状态不只依赖颜色，键盘可完成建项、提交、查看 QA 和导出；弹窗有焦点约束并支持 Escape 关闭。
- 3D 画布提供可理解名称和非 3D 降级信息；动态状态采用克制的 live region，不逐事件轰炸辅助技术。

## 6. 偏离本手册的地方

| 偏离项 | 原因 | 影响与处理 |
| --- | --- | --- |
| 使用 Vinext/Vite/Sites，而非手册默认的官方 Next.js 稳定构建链 | 当前仓库已是 Sites/Cloudflare Workers 兼容工程，迁移会扩大范围并破坏现有托管约束 | 保留 App Router 与 React 编程模型；重点验证 beta 兼容性、Worker 运行限制和真实部署，不套用 Next.js standalone 交付步骤 |
| 暂不引入 TanStack Query、Zustand、React Hook Form | 当前只有一条核心工作流，复杂状态库尚无明确收益 | 先拆分业务状态和 API 层；达到多路由缓存、复杂表单或跨组件共享阈值后再引入 |
| 国际化暂用轻量字典而非 next-intl | 当前单页 Alpha 已有中英文文案，迁移不是本轮首要价值 | 在多页面扩展前拆出统一资源、补持久化和缺键测试；若路由级语言需求出现，再迁移成熟 i18n 方案 |
| 当前页面使用轮询而非 SSE | 前端 SSE 客户端尚未实现，后端接口已存在 | 在 Agent 完整交互阶段补齐 SSE，轮询只保留为恢复与降级；当前实现不视为最终完成 |
| 当前恢复依赖 `/latest`，未使用任务 URL | 单页骨架优先验证了最近任务恢复 | 补任务 ID 路由或查询参数；多用户或多任务环境不得继续使用全局 latest 作为恢复依据 |
| 当前前端没有完整测试脚本 | 工程仍处于 Alpha 骨架阶段 | 补 `typecheck`、Vitest/RTL、Playwright 后，才可达到手册交付门 |
| 移动端不支持完整 3D 编辑 | PRD 明确桌面为创作终端，移动端只用于查看/审批 | 仍需保证核心内容不溢出、状态可读、审批可操作，并在 390/768px 真实验收 |

## 7. 待确认事项

以下事项不阻塞本地 Mock 纵向切片，但会阻塞真实联调、外部测试或正式发布：

1. **前后端线上拓扑**：确认 Sites 前端对应的 FastAPI HTTPS 地址，以及采用同源代理还是跨域 API；据此冻结 CORS、Cookie、CSRF、静态资产地址和流式请求策略。
2. **Vinext/Sites 是否作为 Alpha 正式部署基线**：本文建议继续沿用。如果生产目标改为官方 Next.js Node 运行时，需要单独立项评估兼容、部署和迁移成本，不在业务开发中顺手切换。
3. **外部测试身份与资产访问**：当前无正式登录、租户隔离和对象存储。若 Alpha 要开放给仓库外用户，必须先确定最小认证、私有文件授权、留存与删除方案。
4. **真实 3D 与导出完成门**：确认真实供应商测试预算、最终模型版本、QA 阈值和 Unity 基准资产；没有真实付费链路与 Unity 导入记录时，只能判定“工程链路通过”，不能判定“真实产品效果通过”。

## 8. 适配结论

当前前端技术方向总体适配本项目：React/TypeScript、语义化 Tailwind、集中 API 层、React Three Fiber 和 Sites/Cloudflare Workers 能支撑 Alpha 纵向切片，不需要重建工程。下一步的重点不是继续增加界面，而是补齐统一任务状态映射、SSE 与断线恢复、稳定任务 URL、取消与幂等恢复、真实 QA/导出接口、国际化持久化和前端自动化测试。

在上述缺项完成并通过真实浏览器与后端联调前，当前前端应定义为“可运行的 Alpha 骨架”，不定义为“第一阶段前端已完成”。
