# Server-side URL used by the same-origin /api/v1 proxy in production.
ASSETFORGE_BACKEND_URL=https://your-assetforge-api.example.com

# Optional browser-side override for local development only.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8010/api/v1
