**PRODUCT REQUIREMENTS DOCUMENT**

**AI 游戏素材生成平台**

AssetForge AI（暂定名）｜从创意描述到游戏引擎可验收的 3D 资产

**文档版本：**v0.9 方案评审稿

**创建日期：**2026-08-28

**产品负责人：**待定

**目标平台：**Web 优先；Unity / Unreal / Godot / Roblox / Blender 集成

**参考产品：**Meshy AI（公开页面只读分析）

> **一句话决策** MVP 聚焦浏览器内 3D 游戏资产，不扩展到音乐、音效、视频或完整关卡生成；用“项目规格模板 + 风格套件 + 自动技术 QA + 引擎导出”建立区别于通用 3D 生成器的生产价值。

*本文档中的竞品事实使用【已确认】，推导使用【合理推断】，本产品方案使用【建议设计】。*

# 0. 执行摘要

AssetForge AI 面向独立游戏开发者、小型工作室与技术美术，提供从文本/参考图到可导入游戏引擎的 3D 素材生产链路。产品不只交付“看起来像”的模型，还将多边形预算、拓扑、UV/PBR、缩放轴向、原点、命名、碰撞体、骨骼与许可证写进同一份资产规格，并在导出前给出可解释的就绪度评分。

- 核心用户价值：把建模、贴图、优化、绑定、质检和导出压缩到一个可追踪工作流。

- 核心产品壁垒：批量资产的风格一致性、项目级规格继承、自动技术验收和版本血缘。

- 首发范围：道具、环境件与基础角色；优先支持静态资产，角色绑定/动画作为受控 P0.5。

- 北极星指标：每周每个活跃项目“通过 QA 且成功导出”的资产数。

# 1. 背景、机会与证据边界

## 1.1 需求背景

游戏团队在概念验证阶段需要大量低成本素材，在生产阶段又要求模型满足引擎与平台约束。传统流程的断点不在“生成一个模型”，而在于从概念到可用资产之间仍存在反复修模、贴图、减面、命名、导入和验收。AssetForge AI 的机会是把生成任务变成可复用、可批量、可校验的资产流水线。

## 1.2 本次竞品查看范围

| **证据** | **公开页面** | **可观察事实** | **结论** |
|----|----|----|----|
| 证据-001 | Discover | 首页可输入图片或文字，并直接设置生成质量/消耗；社区按游戏开发、集合、创作者组织素材。 | 【已确认】入口把生成与灵感发现放在同一页面。 |
| 证据-002 | Features | 公开列出 3D Agent、文本/图片转 3D、AI 图像、PBR 贴图、重拓扑、低模、自动绑定、动画、API/MCP/Skill。 | 【已确认】覆盖 3D 生产多阶段。 |
| 证据-003 | Game Development | 公开描述文本/图片生成、PBR、自动绑定、动画、FBX/OBJ/GLB 等导出及 Unity/Unreal/Godot/Roblox/Blender 集成。 | 【已确认】明确服务游戏开发工作流。 |
| 证据-004 | Pricing | 公开方案按月度额度、并发、优先级、重试、许可证、协作、API 与资产留存分层。 | 【已确认】生成额度与生产能力共同构成商业化。 |

> **证据缺口** 未登录、未实际触发生成，因此无法确认 Meshy 的真实成功率、生成耗时分布、失败恢复、版本管理、引擎插件回写、付费转化和内部技术架构。PRD 不把这些未知事项写成竞品事实。

## 1.3 竞品启发与机会判断

| **观察** | **产品含义** | **AssetForge 机会** |
|----|----|----|
| 从输入到导出是一条连续链路 | 用户购买的是节省的总制作时间，而非单次生成。 | 以“通过游戏规格验收”为完成门。 |
| 社区示例与免费素材承担获客 | 可见成果降低首次尝试门槛。 | 按引擎/平台/风格展示可复用资产包与提示词。 |
| 额度、并发、重试和许可证被同时定价 | 稳定性、速度和权利归属是付费价值。 | 把技术失败免费重试、私有项目和团队审批准入付费层。 |
| 插件/API 扩大工作流覆盖 | 生成器需要进入已有 DCC/引擎生态。 | MVP 先做好标准导出包，再按使用量选择 Unity/Unreal 插件。 |

# 2. 产品愿景与定位

## 2.1 愿景

让任何游戏团队都能把一份美术需求单，在数分钟内转化为可审阅、可修订、可追溯、可直接进入引擎的 3D 资产。

## 2.2 定位

> **定位语** 面向没有完整 3D 产能或需要高频试错的游戏团队，AssetForge AI 是“游戏规格优先”的 AI 3D 资产工作台；它生成的不只是模型，而是带版本、质检、许可证和引擎清单的可交付资产包。

## 2.3 差异化支柱

| **支柱** | **用户问题** | **方案** |
|----|----|----|
| Game-ready by design | 模型好看但导入后比例、轴向、材质或面数不合格。 | 创建项目时先选引擎、平台和性能档；生成与优化继承同一规格。 |
| Style Kit | 同一项目中的资产像来自不同游戏。 | 使用参考图、色板、材质语言和禁用词建立项目风格套件，并在批量生成中锁定。 |
| Explainable QA | 用户不知道为什么不能用。 | 就绪度评分拆为几何、材质、规格、绑定、命名、许可证六类，并提供一键修复建议。 |
| Version lineage | 反复生成后找不到被哪次修改破坏的版本。 | 每次生成/优化形成不可变版本，记录父版本、参数、成本与导出历史。 |

## 2.4 非目标

- MVP 不替代 Blender/Maya 的精细手工建模与高级雕刻。

- MVP 不提供完整游戏关卡编辑器、游戏逻辑、Shader Graph 或代码生成。

- MVP 不覆盖 2D UI、音频、音乐、视频和对白；后续以独立产品域评估。

- MVP 不承诺所有生成结果无需人工审美选择；系统负责技术可用性与迭代效率。

# 3. 用户与核心任务

| **角色** | **主要目标** | **核心痛点** | **成功标准** |
|----|----|----|----|
| 独立开发者 / 小团队主创 | 快速做可玩的原型与内容填充。 | 不会完整 3D 流程；预算小；等待外包慢。 | 15 分钟内获得可导入引擎的首个资产。 |
| 技术美术 / 3D 美术 | 批量产出并守住项目技术标准。 | 返修来自面数、UV、轴向、命名和材质不一致。 | 多数资产自动通过规格检查，必要问题可定位。 |
| 制作人 / 美术负责人 | 控制风格、成本、进度和审批。 | 生成过程不可预测，版本与额度难管理。 | 能按项目查看成本、状态、审批与导出记录。 |
| 工具/API 开发者 | 把生成嵌入内部内容流水线。 | 异步任务、失败补偿和格式归一化复杂。 | 有稳定 API、Webhook、幂等与结构化 QA 结果。 |

## 3.1 Jobs to be Done

- 当我只有一句需求或一张概念图时，我想生成多种可比较的 3D 方案，以便快速确定方向。

- 当我已经选中一个模型时，我想按目标平台自动优化与修复，以便不用学习复杂 DCC 操作也能导入引擎。

- 当我要做一组同世界观的资产时，我想锁定风格、材质和技术规格，以便批量结果保持一致。

- 当我把资产交给团队时，我想附带 QA、来源、许可证和版本信息，以便降低上线风险。

# 4. 产品目标、指标与约束

## 4.1 业务与用户目标

| **目标** | **指标** | **Beta 目标** | **口径** |
|----|----|----|----|
| 激活 | 首日核心激活率 | ≥ 45% | 注册后 24 小时内创建项目、生成预览并打开资产详情。 |
| 价值实现 | 首个可导出资产耗时 P50 | ≤ 15 分钟 | 从项目创建到首次通过 P0 QA 并导出。 |
| 技术可用 | 首次生成技术通过率 | ≥ 70% | 无需人工 DCC 修复即可通过必选规格。 |
| 使用深度 | 周活项目导出资产数 P50 | ≥ 3 | 同一项目一周内通过 QA 且成功导出的唯一资产。 |
| 留存 | 创作者 D7 留存 | ≥ 25% | 首周激活用户在第 7 天回访并进行有效创作。 |
| 可靠性 | 任务完成率 | ≥ 97% | 排除用户取消与安全拦截后的生成/优化任务成功率。 |

## 4.2 北极星与护栏

> **北极星指标** Weekly Approved & Exported Assets per Active Project（每周每个活跃项目通过 QA 且导出的资产数）。它同时要求有真实创作、技术验收和下游使用，避免用“生成次数”制造虚假繁荣。

- 成本护栏：单个成功导出资产的推理成本、存储成本与免费重试成本可拆分。

- 质量护栏：用户退款/举报率、重复生成率、QA 误判率和导入失败率不得因增长恶化。

- 安全护栏：侵权投诉、敏感内容拦截漏检和私有资产误公开为零容忍事件。

## 4.3 关键假设

| **假设** | **验证方法** | **失败时调整** |
|----|----|----|
| 用户愿意为技术可用性而非纯视觉质量付费。 | 访谈 12–15 个团队；比较“仅模型”与“带 QA/引擎包”概念测试。 | 缩小 QA 规格，转向更强的生成/编辑体验。 |
| 项目级风格套件能提高批量复用。 | Beta 对照：无 Style Kit 与有 Style Kit 的选中率、重生成率。 | 将风格套件降级为高级模板或团队功能。 |
| 静态道具是最适合的首发品类。 | 按品类统计首轮通过率、导出率和复用率。 | 改为角色概念预览或场景组件化方向。 |

# 5. 范围与优先级

| **模块** | **P0（MVP）** | **P1（V1）** | **P2（后续）** |
|----|----|----|----|
| 输入 | 文本、单图、项目规格、负面词 | 2–8 多视图、草图、项目色板 | 视频/扫描输入 |
| 生成 | 静态道具/环境件，4 候选 | 基础角色、批量套件、局部重生成 | 组合场景、程序化变体 |
| 后处理 | 减面、拓扑、UV/PBR、比例/原点 | 自动 LOD、碰撞体、局部材质编辑 | 平台专用烘焙与 Shader 适配 |
| 角色 | 受控灰度：人形基础绑定 | 四足、动画预设、重定向 | 视频驱动动作、面部绑定 |
| QA | 几何/面数/材质/尺度/命名/许可 | 绑定/动画/LOD/碰撞体与引擎内验证 | 可学习的项目自定义规则 |
| 交付 | GLB/FBX/OBJ + 纹理 + manifest | Unity/Unreal 一键同步 | Godot/Roblox/Maya/3ds Max 插件 |
| 协作 | 私有项目、版本、链接评审 | 角色权限、批注、审批门 | 企业 SSO、审计与私有部署 |

## 5.1 MVP 发布边界

- 首发资产类型：武器、家具、植物、岩石、建筑小件、模块化环境件；不首发复杂布料、头发和写实人脸。

- 首发输出：GLB 为默认预览/下载格式；FBX 面向引擎，OBJ 面向兼容；纹理为 PNG。

- 首发项目模板：Unity URP Mobile、Unity PC、Unreal PC、Roblox/Godot 通用低模。

- 角色能力若自动绑定通过率未达到 80%，则从正式 MVP 移出，仅保留 Early Access。

# 6. 核心用户旅程

**创建项目 → 选择引擎/平台规格 → 输入描述或参考图 → 必要追问与方向选项 → 文字生成 Seedream 道具单图或人物四视图/独立配件并确认 → Tripo 多视图高质量建模与 20K 优化 → 3D 对比 → 自动 QA → 导出**

| **阶段** | **用户动作** | **系统反馈/状态** | **完成门** |
|----|----|----|----|
| 1\. 建项 | 选择模板、单位、目标平台、资产类型。 | 展示默认面数、贴图、轴向与命名规则。 | 项目规格已保存。 |
| 2\. 输入 | 填写提示词、上传参考图、选择 Style Kit。 | 文本模式解析主体/风格/必要细节；缺失信息以单选卡提供推荐方向、简短说明和“其他/自定义”，道具检查材质与结构，人物检查外观、服装与姿态且不问材质。图片模式上传后显示原图缩略图、文件信息和移除/替换操作，再由用户确认“按图生成 3D”以及主体类型。 | 输入安全、关键信息明确且已由用户确认；未完成追问/图片用途确认不得提交生成。 |
| 3\. 生成前确认 | 核对完整需求、主体类型、模型路由、技术目标和预计消耗。 | 道具文字生成 1 张参考图；人物文字生成无遮挡的正/左/后/右完整身体视图，可拆配件单独成图且不拆四肢。确认卡展示实际图片张数与费用。上传图片完全跳过 Seedream；单图人物由 Tripo 补全多视图。 | Seedream 与 Tripo 分别设置独立明确确认门。 |
| 4\. 生成 | 提交生成，可离开页面。 | 标准档为 Tripo P1 + Detailed PBR；高质量档为 v3.1 Detailed 高模 → Smart LowPoly 烘焙到 20K，并显示每个供应商任务阶段。 | 至少 1 个候选可预览。 |
| 5\. 比较 | 旋转、线框、材质通道与并排比较。 | 显示视觉预览与实测技术指标。 | 用户选中一个版本或继续迭代。 |
| 6\. 优化 | 设定目标面数、拓扑、贴图与绑定。 | 异步执行，保留父版本；若初始模型超出规格，以独立优化版本修复并再次实测。 | 生成优化版本且实测值达标。 |
| 7\. QA | 查看就绪度与阻断项，选择自动修复。 | 对比“用户目标”“供应商接受参数”和“文件实测值”，逐项解释。 | 所有 P0 阻断规则通过或被授权豁免。 |
| 8\. 交付 | 选择格式/引擎并下载或同步。 | 生成模型、纹理、manifest、QA、license。 | 导出成功并记录版本。 |

## 6.1 失败与恢复

| **场景** | **系统行为** | **用户恢复入口** |
|----|----|----|
| 参考图主体不清晰 | 预处理阶段暂停，不扣除完整生成额度。 | 裁剪、去背景、补充角度或改用文本。 |
| 文本描述不清晰 | 不创建生成任务，先确认主体方向，再针对缺失的风格和关键细节提出最多 3 个问题；每题提供多个单选项与“其他/自定义”。材质问题仅用于道具/环境件，人物追问外观服装与姿态。 | 用户点选或自定义答案、查看合并后的完整需求，再确认生成。 |
| 生成部分失败 | 保留已成功候选，仅对失败分支重试。 | 免费技术重试；也可用剩余候选继续。 |
| 余额不足 | 提交前计算预计消耗并阻止排队。 | 降低候选数/质量、购买额度或稍后继续。 |
| QA 不通过 | 资产状态为 NEEDS_FIX，不允许标记 Approved。 | 一键修复、手动豁免（需理由）或创建新版本。 |
| 导出失败 | 生成任务不回滚，保留已通过版本。 | 重新打包、切换格式或复制诊断 ID。 |
| 用户取消 | 在可中断阶段停止后续计算并结算实际消耗。 | 从最后持久化版本重新开始。 |

# 7. 功能需求

## 7.1 项目与规格模板

| **ID** | **需求** | **优先级** | **验收标准** |
|----|----|----|----|
| FR-001 | 创建项目并选择引擎、目标平台、单位、轴向、资产类型与性能档。 | P0 | 保存后所有新任务继承规格；修改规格不静默覆盖旧版本。 |
| FR-002 | 提供可编辑的规格预设：三角面预算、拓扑、贴图分辨率、PBR 通道、原点、命名、碰撞体、LOD。 | P0 | 每项有默认值、解释和允许范围；无效组合不可提交。 |
| FR-003 | 创建 Style Kit：参考图、色板、材质关键词、形状语言、负面词。 | P1 | 生成任务记录 Style Kit 版本；修改后可选择是否影响后续任务。 |

## 7.2 输入与生成

| **ID** | **需求** | **优先级** | **验收标准** |
|----|----|----|----|
| FR-010 | 支持中文/英文文本输入和单张 PNG/JPG/WebP 参考图。 | P0 | 图片最大 20 MB，按真实图片内容校验格式与尺寸；上传后展示用户原图、文件名、分辨率和大小，可替换/移除；原图不冒充 3D 候选结果。 |
| FR-011 | 提示词助手抽取主体、风格、年代、比例、视角和禁用项；道具/环境件补充材质和结构，人物/角色补充外观、服装与姿态；信息不足时主动追问。 | P0 | 人物输入不得询问“主要材质”；先确认主体方向，每个问题提供 4 个明确选项、说明和“其他/自定义”，单轮最多 3 问；用户回答并确认前不得创建生成任务或产生模型费用。 |
| FR-012 | 单次生成 1/2/4 个候选，默认 4 个。 | P0 | 候选可独立成功/失败；成功候选出现后即可预览。 |
| FR-013 | 显示预计额度、队列等级和阶段进度。 | P0 | 提交前确认预计成本；运行中显示阶段，不展示伪精确百分比。 |
| FR-014 | 支持批量资产清单与共享 Style Kit。 | P1 | 每个条目独立追踪，批次可暂停、取消和重试失败项。 |
| FR-015 | 所有真实 3D 调用前显示生成确认卡。 | P0 | 确认卡包含最终输入、主体类型、模型路由、候选数、技术目标和费用风险；用户点击确认前不创建任务。 |
| FR-016 | 区分请求规格、供应商支持参数和返回文件实测值。 | P0 | 不以 Prompt 中的“1 万面”代替检测；超预算候选标记 NEEDS_FIX，经减面/重拓扑后创建子版本并重新 QA。 |

## 7.3 预览、比较与版本

| **ID** | **需求** | **优先级** | **验收标准** |
|----|----|----|----|
| FR-020 | 3D 查看器支持旋转/缩放、环境光、背景、线框、法线、UV 与材质通道。 | P0 | 桌面浏览器保持可交互；大模型提供降级预览。 |
| FR-021 | 最多 4 个候选并排比较，展示面数、材质数、贴图和初步 QA。 | P0 | 用户可明确选中一个候选进入资产详情。 |
| FR-022 | 所有生成、编辑、优化形成不可变版本树。 | P0 | 版本记录父版本、参数、模型版本、成本、时间与操作者。 |
| FR-023 | 支持收藏、标签、集合、搜索和软删除。 | P0 | 删除进入回收站；恢复不丢版本与导出记录。 |

## 7.4 后处理与角色

| **ID** | **需求** | **优先级** | **验收标准** |
|----|----|----|----|
| FR-030 | 按目标面数重拓扑，支持三角/四边面偏好。 | P0 | 输出在预算容差内；原版与优化版同时保留。 |
| FR-031 | 生成/重做 UV 和 PBR 纹理：Base Color、Normal、Roughness、Metallic、AO。 | P0 | 缺失通道被标记；贴图尺寸和色彩空间写入 manifest。 |
| FR-032 | 自动调整单位、轴向、原点、枢轴与包围盒。 | P0 | 预览与导出使用同一变换；可撤销到父版本。 |
| FR-033 | 人形基础自动绑定与动画预设。 | P0.5 | 骨骼层级、权重异常和循环动画通过 QA 后才可导出为 Ready。 |
| FR-034 | 自动生成 LOD 与碰撞体。 | P1 | 每级 LOD 有面数与切换距离；碰撞体可在预览中开关。 |

## 7.5 QA、审批与导出

| **ID** | **需求** | **优先级** | **验收标准** |
|----|----|----|----|
| FR-040 | 计算六维就绪度：几何、规格、材质、绑定、命名、许可证。 | P0 | 每项显示 Pass/Warning/Block、证据值和修复建议。 |
| FR-041 | 支持自动修复与带理由的规则豁免。 | P0 | 自动修复创建新版本；豁免记录操作者、时间和理由。 |
| FR-042 | 导出 GLB/FBX/OBJ 与纹理、manifest、QA 报告、license。 | P0 | 打包内容与用户选择一致；下载前校验文件完整性。 |
| FR-043 | 通过链接进行评论与批准。 | P0 | 评论锚定版本；批准只对特定版本有效。 |
| FR-044 | Unity/Unreal 插件同步。 | P1 | 同步后回传成功/失败和目标路径，不静默覆盖已有资产。 |

## 7.6 额度、通知与运营

| **ID** | **需求** | **优先级** | **验收标准** |
|----|----|----|----|
| FR-050 | 额度按阶段计费，技术失败不向用户收取完整费用。 | P0 | 账单展示项目、任务、阶段、扣费与退款。 |
| FR-051 | 任务完成/失败/余额预警通知。 | P0 | 站内通知可配置；邮件为用户主动开启。 |
| FR-052 | 社区案例可浏览并复制提示词/规格。 | P1 | 仅明确公开的资产进入社区；私有为默认。 |
| FR-053 | 管理后台查看任务、成本、模型版本、投诉和内容安全结果。 | P0 | 操作有审计日志；客服不默认访问私有资产内容。 |

# 8. 交互与信息架构

## 8.1 一级导航

| **区域**   | **核心内容**                                | **首发** |
|------------|---------------------------------------------|----------|
| 发现       | 案例、模板、公开资产、教程、搜索。          | 精简版   |
| 工作台     | 项目、资产、批次、任务中心。                | P0       |
| 生成器     | 输入、结构化参数、Style Kit、成本预估。     | P0       |
| 资产详情   | 3D 预览、版本、指标、优化、QA、评论、导出。 | P0       |
| 团队       | 成员、角色、额度、审批与项目访问。          | P1       |
| API / 集成 | 密钥、Webhook、用量、插件下载和诊断。       | P1       |

## 8.2 关键页面布局

- 生成页：左侧输入与规格；中间候选画布；右侧任务状态与成本。移动端只支持查看/审批，不承诺完整 3D 编辑。

- 资产详情：上方版本与状态；中心 3D 查看器；右侧六维 QA；底部参数、来源、评论与导出历史。

- 任务中心：按项目/类型/状态筛选，明确 Queued、Running、Needs Fix、Ready、Failed、Cancelled。

- 导出面板：先选目标引擎，再自动给出格式、轴向、单位、贴图打包和命名方案；高级用户可覆盖。

## 8.3 空状态与反馈原则

| **状态** | **必须呈现** | **禁止** |
|----|----|----|
| 空项目 | 示例任务、推荐规格和最短完成路径。 | 只显示空白画布。 |
| 运行中 | 当前阶段、已持久化结果、取消边界、预计成本。 | 伪造精确倒计时或思维链。 |
| 部分成功 | 已成功候选与失败原因并存。 | 因一个候选失败而隐藏全部结果。 |
| QA 失败 | 阻断规则、检测值、目标值、影响和修复入口。 | 只显示低分或“不可用”。 |
| 额度不足 | 所需/剩余额度和降配方案。 | 先运行后弹出欠费。 |

# 9. 工作流、状态机与完成门

## 9.1 生成任务状态

**DRAFT → VALIDATING → QUEUED → PREPROCESSING → GEOMETRY → TEXTURING → POST_PROCESSING → QA → READY / NEEDS_FIX / FAILED / CANCELLED**

| **状态** | **含义** | **允许操作** | **完成条件** |
|----|----|----|----|
| VALIDATING | 检查输入、权限、安全、余额和参数。 | 返回修改、取消。 | 全部检查通过并锁定预计额度。 |
| QUEUED | 任务已持久化等待资源。 | 取消、提升优先级。 | 获得执行资源。 |
| GEOMETRY / TEXTURING | 生成几何/贴图；候选可部分返回。 | 查看阶段、取消未开始分支。 | 所需产物持久化并通过基础完整性检查。 |
| POST_PROCESSING | 重拓扑、UV、变换、绑定等。 | 取消后续步骤。 | 后处理产物与父版本血缘写入。 |
| QA | 执行规则并生成结构化报告。 | 无。 | 所有规则产生确定结果，不允许缺失值伪装为 Pass。 |
| READY | 所有阻断规则通过或获得授权豁免。 | 导出、批准、创建新版本。 | 产物存在、状态落库、QA 完整、用户批准（若项目要求）。 |
| FAILED | 技术或业务条件使任务终止。 | 查看诊断、重试、复制任务。 | 失败原因、计费结果和恢复入口已写入。 |

## 9.2 幂等与重试

- 客户端提交包含 idempotency_key；相同用户、相同键在 24 小时内只创建一个任务。

- 系统只重试可恢复的网络、资源和模型服务错误；内容安全、余额不足、参数无效不自动重试。

- 候选级失败独立补偿；已成功并持久化的候选不重复生成。

- 所有重试记录 attempt、触发方、模型版本、扣费/退款和最终状态。

# 10. 输入输出契约与资产包

## 10.1 生成请求（概念契约）

| **字段** | **类型** | **必填** | **说明** |
|----|----|----|----|
| project_id | UUID | 是 | 继承项目规格和权限。 |
| asset_type | enum | 是 | prop / environment / vehicle / humanoid / quadruped。 |
| prompt | string | 条件 | 与 reference_images 至少一个存在。 |
| reference_images | array | 条件 | P0 为 1 张；P1 为 2–8 张。 |
| style_kit_version | UUID | 否 | 锁定到提交时版本。 |
| spec_profile | object | 是 | 引擎、平台、面数、纹理、单位、轴向、拓扑等。 |
| candidate_count | 1/2/4 | 是 | 默认 4。 |
| quality_tier | draft/standard/high | 是 | 影响耗时与额度。 |
| idempotency_key | string | 是 | 防重复提交。 |

## 10.2 标准导出包

**asset-name_v012.zip**

- model/asset-name.fbx（或 .glb / .obj）

- textures/base_color.png、normal.png、roughness.png、metallic.png、ao.png

- preview/turntable.png 与 thumbnail.png

- manifest.json：版本、单位、轴向、包围盒、三角面、材质、纹理色彩空间、骨骼/动画、来源。

- qa_report.json：规则版本、检测值、结果、豁免和修复记录。

- license.txt：用户权利、输入来源声明、生成时间和平台条款版本。

## 10.3 API 最小能力

- POST /v1/generation-tasks：创建任务；GET /tasks/{id}：状态与候选；POST /tasks/{id}/cancel。

- POST /v1/assets/{version}/optimize；POST /qa；POST /exports；GET /exports/{id}/download。

- Webhook：task.partial_succeeded、task.completed、task.failed、asset.qa_updated、export.ready。

- Webhook 使用签名、重放保护和递增事件序号；下载 URL 短期有效。

# 11. AI 能力与技术架构（能力级）

> **架构原则** 以下是本产品需要具备的能力分层，不推断 Meshy 的隐藏实现，也不绑定具体模型厂商。生成、后处理与 QA 必须可独立替换、观测和回滚。

**Web / 插件 / API → API 网关与权限 → 工作流编排与队列 → 输入预处理 → 3D 生成路由 → 贴图/重拓扑/绑定 → 技术 QA → 资产与版本存储 → 导出打包 / 通知 / 分析**

| **层** | **职责** | **关键要求** |
|----|----|----|
| 体验层 | 工作台、3D 预览、任务中心、审批、插件。 | 同一资产状态在各端一致；私有默认。 |
| 控制层 | 权限、额度预授权、工作流、重试、幂等、Webhook。 | 状态持久化先于通知；支持候选级补偿。 |
| 生成层 | 输入理解、图像预处理、几何/贴图/绑定模型路由。 | 记录模型版本、参数、随机种子/可复现标识和成本。 |
| 后处理层 | 重拓扑、UV、烘焙、LOD、碰撞体、坐标变换。 | 每一步产生新版本或可验证中间产物。 |
| QA 层 | 规则引擎、网格检测、纹理检测、绑定/动画测试。 | 规则版本化；Pass 必须有检测值。 |
| 资产层 | 对象存储、元数据、版本树、导出包、留存。 | 校验和、加密、租户隔离、软删除与生命周期。 |
| 治理层 | 安全、版权、审计、投诉、质量与成本分析。 | 可阻断、可追溯、最小权限。 |

## 11.1 模型路由与质量门

- 按资产类型、输入类型、质量档和目标平台选择生成路线；同一路线可灰度多个模型版本。

- 视觉评分只用于排序候选，不能替代几何完整性、UV、面数、轴向等确定性检查。

- 模型升级必须通过固定基准集：道具、环境件、硬表面、低模角色和失败样本；比较通过率、成本、耗时与风格偏移。

- 用户选中的版本与后续编辑形成反馈信号；禁止把私有素材默认用于训练。

# 12. 自动 QA 规则

| **类别** | **P0 检查** | **默认结果** |
|----|----|----|
| 几何 | 文件可解析、非空网格、法线、非流形边、破面、自交、重复面、孤立顶点。 | 结构损坏为 Block；轻微几何问题为 Warning。 |
| 规格 | 三角面预算、包围盒、单位、轴向、原点、枢轴、材质数。 | 超预算 \>10%、轴向/单位错误为 Block。 |
| 材质 | PBR 通道完整、分辨率、色彩空间、UV 覆盖/越界、贴图引用。 | 缺少必选贴图或断链为 Block。 |
| 绑定 | 骨骼层级、根骨、权重归一、极端变形、动画帧和循环。 | 角色启用绑定时，关键错误为 Block。 |
| 命名 | 文件、对象、材质、纹理、骨骼符合模板；禁止特殊字符。 | 可自动修复则 Warning，否则 Block。 |
| 许可证 | 输入声明、项目可见性、输出权利、豁免和投诉状态。 | 权利不明或投诉冻结为 Block。 |

## 12.1 就绪度分数

Ready Score = 30% 几何 + 25% 规格 + 20% 材质 + 10% 绑定 + 5% 命名 + 10% 许可证。任何 Block 均使资产状态保持 NEEDS_FIX；总分只用于排序与解释，不得覆盖硬性阻断。

## 12.2 人工豁免

- 只有 Owner、Art Lead 或 Technical Artist 角色可以豁免项目规则。

- 豁免必须选择原因、填写说明、设置适用版本；新版本不自动继承高风险豁免。

- 许可证、安全、恶意文件和不可解析网格不可豁免。

# 13. 非功能需求

| **领域** | **要求** |
|----|----|
| 可用性 | 核心 API 月可用性 ≥99.9%；任务状态查询可独立于生成服务工作。 |
| 性能 | 普通页面 LCP P75 ≤2.5s；3D 预览首帧 P75 ≤5s；任务提交响应 P95 ≤800ms（不含生成）。 |
| 任务可靠性 | 排除业务拦截后的完成率 ≥97%；所有任务可关联诊断 ID；重复提交不重复扣费。 |
| 扩展性 | 队列按租户与优先级公平调度；生成并发与 Web/API 服务解耦。 |
| 安全 | 传输与静态加密、短时签名 URL、租户隔离、最小权限、依赖与上传文件扫描。 |
| 隐私 | 项目默认私有；可配置留存与删除；企业支持更严格区域与审计要求。 |
| 可访问性 | 核心 Web 流程达到 WCAG 2.1 AA；键盘可完成建项、提交、查看 QA 和导出。 |
| 兼容性 | 桌面 Chrome/Edge/Safari 最近两个主要版本；Firefox 尽力支持；移动端为查看与审批。 |
| 可观测性 | 每阶段有结构化日志、指标和 trace；模型版本、成本、延迟、错误码可关联。 |

# 14. 数据模型与权限

| **实体** | **关键字段** | **关系** |
|----|----|----|
| Workspace | owner、plan、credits、retention_policy | 包含成员与项目。 |
| Project | spec_profile、style_kit、visibility、approval_policy | 包含资产与任务。 |
| Asset | type、status、approved_version_id、tags | 拥有多个版本。 |
| AssetVersion | parent_id、inputs、params、model_version、qa_report、checksum | 不可变；派生导出。 |
| GenerationTask | state、attempt、cost、idempotency_key、diagnostic_id | 产生一个或多个版本。 |
| Export | target、format、status、package_url、manifest | 绑定一个资产版本。 |
| Review | version_id、comment、decision、actor、timestamp | 评论/批准不跨版本。 |
| LicenseRecord | input_declaration、terms_version、commercial_rights、complaint_state | 绑定输入与输出版本。 |

## 14.1 权限角色

| **角色** | **能力**                                        |
|----------|-------------------------------------------------|
| Owner    | 计费、成员、项目、API、安全、导出、豁免与删除。 |
| Art Lead | 管理 Style Kit、规格、审批、评论和高风险豁免。  |
| Creator  | 生成、编辑、优化、QA、导出被授权项目。          |
| Reviewer | 查看、评论、批准/拒绝；不能生成或扣费。         |
| Viewer   | 只读预览与下载已授权资产。                      |

# 15. 安全、版权与合规

- 输入责任：上传前提示用户确认拥有必要权利；记录来源声明，不默认公开或训练。

- 内容安全：拦截违法、性化未成年人、极端暴力、仇恨与其他平台禁止内容；疑似规避进入人工复核。

- 角色/IP：对明确的受保护角色、品牌与艺术家模仿请求给出限制或替代建议；保留投诉和下架流程。

- 资产溯源：manifest 与 license 记录输入类型、生成时间、模型/规则版本、用户声明和后续编辑。

- 隐私：私有项目不进入社区；客服访问需要显式授权与审计；删除请求覆盖资产、导出包与可识别派生物。

- 安全导出：上传模型、纹理和压缩包执行类型校验、解压炸弹防护和恶意内容扫描。

> **发布阻断** 在用户权利、模型商业条款、社区公开授权、投诉下架和数据训练选择未通过法务评审前，不开放公开社区投稿与资产商店。

# 16. 商业化与额度（待验证方案）

竞品公开页面已经验证“额度 + 并发 + 重试 + 许可证 + 协作/API”的组合定价逻辑【已确认，证据-004】。以下价格与额度为【建议设计】，上线前必须用成本测算、支付意愿访谈和价格实验校准。

| **方案** | **目标用户** | **建议权益** | **价格假设** |
|----|----|----|----|
| Free | 首次体验/学生 | 100 试用额度、1 并发、标准质量、私有草稿、有限留存。 | ¥0 |
| Indie | 独立开发者 | 1,200 月度额度、4 并发、高质量、无限下载、商业使用、技术失败免费重试。 | ¥99/月 |
| Studio | 小型工作室 | 8,000 共享额度、20 并发、团队空间、审批、Style Kit、API/插件。 | ¥699/月起 |
| Enterprise | 中大型团队 | 自定义并发/留存、SSO、审计、SLA、区域化与采购支持。 | 询价 |

## 16.1 额度原则

- 提交前显示预计额度；按实际完成阶段结算，平台技术故障不扣完整额度。

- 质量档、候选数和后处理步骤分开计价，让用户能降配而不是被迫放弃。

- 避免以不可理解的单一数字隐藏成本；账单展示项目、资产、阶段和退款。

- 建立成本上限：当任务预测成本异常时暂停并请求用户确认。

# 17. 数据埋点与实验

| **事件** | **关键属性** | **用途** |
|----|----|----|
| project_created | engine、platform、spec_profile | 激活与模板需求。 |
| generation_submitted | input_type、asset_type、candidate_count、quality、estimated_credits | 漏斗与成本。 |
| candidate_previewed/selected | candidate_id、rank、time_to_select | 候选质量与排序。 |
| optimization_started/completed | operation、target_spec、cost、result | 后处理价值。 |
| qa_completed | score、block_count、rule_version、auto_fixable | 技术通过率与误判。 |
| asset_approved | role、version、waiver_count | 团队完成门。 |
| export_started/completed | target_engine、format、package_size、error_code | 真实价值与兼容性。 |
| credit_spent/refunded | stage、amount、reason | 毛利、透明度与故障成本。 |

## 17.1 首批实验

1.  候选数量：默认 2 vs 4，比较选中率、时间、成本与放弃率。

2.  QA 呈现：单一分数 vs 分项阻断，比较修复完成率与导出成功率。

3.  建项方式：先选项目规格 vs 生成后再选，比较首次导出耗时与导入失败。

4.  Style Kit：轻量色板/关键词 vs 完整参考集，比较批量选中率和重生成率。

# 18. 交付计划与里程碑

| **阶段** | **周期** | **交付** | **退出条件** |
|----|----|----|----|
| Discovery | 第 1–4 周 | 用户访谈、竞品基准集、规格模板、成本模型、交互原型。 | 目标品类与付费价值通过评审。 |
| Alpha | 第 5–10 周 | 建项、文本/单图生成、候选预览、版本、基础 QA、GLB/FBX 导出。 | 内部基准集首次技术通过率 ≥60%。 |
| Closed Beta | 第 11–16 周 | 重拓扑/PBR、额度、链接评审、监控、20–30 个团队试用。 | 任务完成率 ≥95%，P50 首次导出 ≤20 分钟。 |
| Public Beta | 第 17–22 周 | Style Kit、批量、角色 Early Access、商业计划与客服。 | 任务完成率 ≥97%，严重安全/隐私事故为 0。 |
| V1 | 第 23 周后 | Unity/Unreal 集成、团队审批、API、更多模板。 | 北极星和留存达到 Beta 目标。 |

## 18.1 最小团队建议

- 产品 1、设计 1、前端/3D Web 2、后端/平台 2、生成/3D 算法 2–3、技术美术 1、QA 1、数据/安全兼职支持。

- 技术美术必须从 Discovery 参与，负责规格模板、基准资产和引擎验收，不应只在发布前补检。

# 19. 风险与应对

| **风险** | **影响/概率** | **早期信号** | **应对** |
|----|----|----|----|
| 视觉不错但技术不可用 | 高/高 | 重生成多、导出少、QA 阻断集中。 | 静态道具优先；确定性后处理；真实引擎基准测试。 |
| 生成成本压垮毛利 | 高/中 | 单资产成本与失败重试上升。 | 候选渐进返回、分档质量、模型路由、阶段缓存与成本上限。 |
| 风格一致性不足 | 高/中 | 批量选中率低、用户频繁改提示词。 | Style Kit、参考锁定、批次对照与可学习项目偏好。 |
| 版权/隐私投诉 | 高/中 | 热门 IP 请求、误公开或投诉增长。 | 私有默认、限制策略、溯源、下架、训练选择与审计。 |
| 插件维护成本过高 | 中/高 | 引擎版本变化导致导入失败。 | 先标准导出包；按需求排序插件；兼容矩阵和自动回归。 |
| 额度不透明引发信任问题 | 中/中 | 退款、客服与放弃率上升。 | 提交前预估、分阶段账单、技术失败自动退款。 |
| AI 模型升级造成回归 | 高/中 | 相同基准集质量/风格突然下降。 | 版本锁定、灰度、基准集、回滚与用户可见版本信息。 |

# 20. 发布验收清单

- 核心成功路径：建项 → 生成 → 选中 → 优化 → QA → 导出，在受支持浏览器和四个规格模板中均通过。

- 每种失败都有明确错误码、诊断 ID、扣费结果和恢复入口；不存在无限 Running。

- 资产 Ready 同时满足：文件存在、版本持久化、QA 完整、状态落库、必要审批完成。

- GLB/FBX 导入 Unity 与 Unreal 的尺度、轴向、材质与命名符合模板；抽样资产通过人工技术美术复核。

- 私有项目在搜索、社区、分享和客服后台中均不会被未授权访问。

- 额度重复扣费、重复任务、退款失败和导出链接越权均有自动化测试。

- 内容安全、投诉、删除、数据保留和服务条款已通过法务/安全评审。

- 监控覆盖成功率、阶段延迟、成本、模型版本、QA 结果、导出错误和队列积压。

# 21. 待决策问题

| **优先级** | **问题** | **建议决策门** |
|----|----|----|
| P0 | 首发是否包含角色自动绑定，还是仅作为 Early Access？ | Alpha 基准集通过率达到 80% 且失败可解释。 |
| P0 | 免费计划是否允许商业使用，是否要求署名？ | 法务与增长共同评估归因、转化和社区供给。 |
| P0 | 首发市场以中国开发者还是全球开发者为主？ | 影响支付、数据区域、语言、社区与引擎优先级。 |
| P1 | Unity 与 Unreal 哪一个先做原生插件？ | 根据 Beta 导出事件与访谈选择，不凭偏好。 |
| P1 | 是否允许用户选择底层模型？ | 先验证高级用户需求；默认保持任务目标导向。 |
| P1 | 社区是增长入口还是独立资产市场？ | 先验证浏览→复制→生成转化，再决定交易与分成。 |

# 附录 A｜竞品证据台账与来源

*观察时间为 2026-08-28（Asia/Shanghai）。仅使用 Meshy 官方公开页面；价格、额度和功能会变化，复用前应重新核验。*

**证据-001 Meshy Discover / Community（访问：2026-08-28）—** [<u>打开 Meshy 官方页面</u>](https://www.meshy.ai/ja/discover)

**证据-002 Meshy Features（访问：2026-08-28）—** [<u>打开 Meshy 官方页面</u>](https://www.meshy.ai/ja/features)

**证据-003 Meshy for Game Development（访问：2026-08-28）—** [<u>打开 Meshy 官方页面</u>](https://www.meshy.ai/ja/use-cases/free-game-assets)

**证据-004 Meshy Pricing（访问：2026-08-28）—** [<u>打开 Meshy 官方页面</u>](https://www.meshy.ai/ja/pricing)

**补充来源 Meshy API（访问：2026-08-28）—** [<u>打开 Meshy 官方页面</u>](https://www.meshy.ai/ja/api)

## 证据解释

- 【已确认】公开页面直接显示的功能、流程、许可证、格式、集成与价格层级。

- 【合理推断】由多个公开事实共同支持，但无法从未登录页面验证的产品机制。

- 【建议设计】为 AssetForge AI 提出的产品、交互、架构和商业化方案，不代表 Meshy 当前实现。

- 【未知】真实生成质量、失败率、状态一致性、版本能力、插件回写和内部供应商/技术栈。

# 附录 B｜术语

| **术语** | **定义** |
|----|----|
| PBR | 基于物理的渲染材质体系，常见通道包括 Base Color、Normal、Roughness、Metallic、AO。 |
| Retopology | 重建模型拓扑，以获得更可控的面数、边流和变形质量。 |
| LOD | Level of Detail；按距离/性能切换不同复杂度模型。 |
| Style Kit | 项目级风格约束集合：参考图、色板、材质词、形状语言、负面词与版本。 |
| Ready Score | 技术就绪度解释分数；任何 Block 仍然阻止 Ready，不由总分覆盖。 |
| Asset Manifest | 随导出包交付的结构化资产清单与技术元数据。 |
